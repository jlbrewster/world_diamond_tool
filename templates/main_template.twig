{% extends "base.twig" %}

    {% block marquee %}<!-- the product pages will overrride the base template's header here  -->
        {% if product == false %}
          {{ parent() }}  
          {% else %}    
            <h1>World Diamond Tool</h1>
        {% endif %}
    {% endblock %} 

    {% block menu %}  
        {{ parent() }}              
    {% endblock %}
      
   
    {% block footer %}
    {% endblock %}